The dropbox.sql file is the export of MySQL database.

Enter the below commands on the root of the project folder in separate CMDs/Terminals

Terminal 1 (Node):
npm install
nodemon app.js

Terminal 2 (React):
npm start
